# Knowledge Dependency Engine Report

## Overview
The **Knowledge Dependency Engine** builds node-edge dependency trees tracing relationships across shastric rules, elemental balances, and chakra nodes.

## Node & Edge Types
- **Node Types**: `RULE`, `SOURCE`, `ELEMENT`, `CHAKRA`, `REMEDY`.
- **Edge Types**: `DEPENDS_ON`, `CONTRADICTS`, `DERIVED_FROM`, `ENHANCES`, `MITIGATES`.

## Cross-Domain Links
Maps Vastu zone placements (e.g. South-East Fire / Agni) directly to corresponding Chakra energy nodes (Manipura) and elemental balancing remedies.
